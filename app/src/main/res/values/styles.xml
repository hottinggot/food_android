<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorWhite</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/colorBlack</item>
    </style>
    <style name="TextFontStyle" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:fontFamily">@font/app_font</item>
    </style>
    <style name="TextViewFontStyle" parent="Widget.AppCompat.TextView">
        <item name="android:fontFamily">@font/app_font</item>
    </style>

</resources>